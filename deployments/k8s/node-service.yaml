apiVersion: v1
kind: Service
metadata:
  name: soroban-encrypt-node
spec:
  selector:
    app: soroban-encrypt-node
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
